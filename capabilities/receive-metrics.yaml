naftiko: 1.0.0-alpha2
info:
  label: Thanos Receive API — Metrics
  description: 'Thanos Receive API — Metrics. 1 operations. Lead operation: Thanos Prometheus Metrics. Self-contained Naftiko
    capability covering one Thanos business surface.'
  tags:
  - Thanos
  - Metrics
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    THANOS_API_KEY: THANOS_API_KEY
capability:
  consumes:
  - type: http
    namespace: receive-metrics
    baseUri: http://localhost:10902
    description: Thanos Receive API — Metrics business capability. Self-contained, no shared references.
    resources:
    - name: metrics
      path: /metrics
      operations:
      - name: getreceivemetrics
        method: GET
        description: Thanos Prometheus Metrics
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: receive-metrics-rest
    port: 8080
    description: REST adapter for Thanos Receive API — Metrics. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/metrics
      name: metrics
      description: REST surface for metrics.
      operations:
      - method: GET
        name: getreceivemetrics
        description: Thanos Prometheus Metrics
        call: receive-metrics.getreceivemetrics
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: receive-metrics-mcp
    port: 9090
    transport: http
    description: MCP adapter for Thanos Receive API — Metrics. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: thanos-prometheus-metrics
      description: Thanos Prometheus Metrics
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: receive-metrics.getreceivemetrics
      outputParameters:
      - type: object
        mapping: $.
